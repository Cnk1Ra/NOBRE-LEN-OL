import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'
import { Providers } from '@/components/providers'
import { Toaster } from '@/components/ui/toaster'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'Dash On Delivery | Dashboard COD',
  description: 'Dashboard profissional para operações de Cash on Delivery - Tracking, métricas, gestão financeira e operacional',
  keywords: ['COD', 'Cash on Delivery', 'Dashboard', 'E-commerce', 'Tracking', 'Analytics'],
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="pt-BR" suppressHydrationWarning>
      <body className={inter.className}>
        <Providers>
          {children}
          <Toaster />
        </Providers>
      </body>
    </html>
  )
}
